# Ox.SleepingGods.API - PHP Sync service

This is a PHP 8 implementation of the [sync service](../Ox.SleepingGods.API/README.md).

This version is useful if you have PHP already available on your server/NAS and don't want to faff about with 
socketed services etc.  The interface functionality is the same as the C# version.